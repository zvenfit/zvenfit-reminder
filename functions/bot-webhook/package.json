{
  "name": "@zvenfit-reminder/bot-webhook",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build --workspace=@zvenfit-reminder/shared && esbuild src/index.ts --bundle --platform=node --target=node22 --format=esm --outfile=dist/index.js --external:ydb-sdk --external:@yandex-cloud/nodejs-sdk --banner:js=\"import { createRequire } from 'module'; const require = createRequire(import.meta.url);\" && node scripts/prepare-deploy.mjs && cd dist && npm install --omit=dev --no-package-lock",
    "dev": "npm run build --workspace=@zvenfit-reminder/shared && node --env-file=../../.env --import tsx src/dev.ts",
    "typecheck": "npm run build --workspace=@zvenfit-reminder/shared && tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@zvenfit-reminder/shared": "*",
    "grammy": "^1.45.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "esbuild": "^0.28.2",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
