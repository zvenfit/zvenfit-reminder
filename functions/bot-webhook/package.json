{
  "name": "@payments-reminder/bot-webhook",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build --workspace=@payments-reminder/shared && esbuild src/index.ts --bundle --platform=node --target=node20 --format=esm --outfile=dist/index.js --external:ydb-sdk --external:@yandex-cloud/nodejs-sdk --banner:js=\"import { createRequire } from 'module'; const require = createRequire(import.meta.url);\" && node scripts/prepare-deploy.mjs && cd dist && npm install --omit=dev --no-package-lock",
    "typecheck": "npm run build --workspace=@payments-reminder/shared && tsc --noEmit"
  },
  "dependencies": {
    "@payments-reminder/shared": "*",
    "grammy": "^1.35.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "esbuild": "^0.24.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
